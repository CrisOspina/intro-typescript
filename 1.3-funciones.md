``` typescript
function suma(v1:number, v2:number) : number {
  return v1 + v2;
}

suma(2, 4);

function opcional(nombre?:string) { }
  opcional();
  opcional("cris");

function(nombre, apellido:string, edad:number):void {
   //no podemos retornar nada, except undefined o null
}

```